/**
 * Login/Register functionality.
 */
export class AccountMenu extends Menu {
    setAccount(account: any): void;
    /**
     * Set the profile picture with the given data url.
     * @param {*} dataUrl
     */
    setProfilePicture(dataUrl: any): void;
}
import { Menu } from "./Menu";
