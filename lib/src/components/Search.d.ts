/**
 * Search Box
 */
export class SearchBar extends HTMLElement {
    connectedCallback(): void;
    search(query: any, paths: any, fields: any, language: any, offset: any, pageSize: any, snippetLenght: any): void;
}
